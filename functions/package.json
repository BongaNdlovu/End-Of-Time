{
  "name": "end-of-time-functions",
  "engines": {
    "node": "20"
  },
  "main": "index.js",
  "scripts": {
    "build": "node smoke.js",
    "deploy": "npm run build && firebase deploy --only functions"
  },
  "dependencies": {
    "dotenv": "^17.2.3",
    "firebase-admin": "^12.0.0",
    "firebase-functions": "^7.0.2",
    "node-fetch": "^3.3.2",
    "nodemailer": "^7.0.5",
    "stripe": "^14.0.0"
  },
  "devDependencies": {
    "typescript": "^5.4.0"
  }
}
